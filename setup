Set up the ssh - between the PC and Github account

git init
git add .
git commit -m ""
#Adding remote link
git remote add origin git@github.com:username/new_repo
#Push the new repo to Github
git push -u origin master
